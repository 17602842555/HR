# Known Commercial Gaps

This register tracks gaps that prevent declaring the OA system production-ready. Keep every row owned, dated, and tied to objective evidence. Remove a row only after the exit criteria are proven in the target environment.

| ID | Status | Owner | Target Date | Gap | Exit Criteria |
| --- | --- | --- | --- | --- | --- |
| GAP-001 | Mitigated | Deployment lead | 2026-05-30 | Local PostgreSQL, API, frontend proxy, database migrations, seed, commercial smoke, and API-required Playwright have been proven in an isolated CI database on this machine. | `DATABASE_URL=postgresql://oa:***@127.0.0.1:55432/oa_commercial?schema=public` with isolated `SERVER_PORT=8788` and `E2E_PORT=5176` passed `npm run ci:commercial`: migration deploy, seed, API readiness, `npm run smoke:commercial`, API-required Playwright, and archived evidence under `commercial-evidence/ci-20260530T024937Z/`. Keep rerunning equivalent target-environment CI after infrastructure changes. |
| GAP-002 | Open | DevOps lead | 2026-06-05 | Docker CLI is unavailable in the current local environment, so the Compose acceptance drill has not run here; a manual `.github/workflows/commercial-drill.yml` runner path now exists for Docker-capable CI. Docker-free local recovery passed against `oa_commercial_ci` and refreshed `commercial-evidence/latest-local-recovery-drill-summary.json`, but that evidence is diagnostic only. | Docker Desktop or a CI runner with Docker is available; `npm run drill:commercial` or the `commercial-drill` workflow completes and archives database backup, file-storage backup, restore logs, checksums, `drill-summary.json`, `commercial-evidence/latest-drill-summary.json`, and pre/post-restore smoke evidence; `npm run validate:drill-evidence -- commercial-evidence/latest-drill-summary.json --json` and release evidence `drill-evidence` pass. Local `commercial-local-recovery-drill` evidence is useful supporting evidence but does not close this gap. |
| GAP-003 | Open | Security lead | 2026-06-05 | Production secrets, origin values, and Cloudflare Tunnel backend values are templates only; protected workflow paths now exist to validate real secret-managed inputs without committing them. | Production secret store provides non-placeholder `POSTGRES_PASSWORD`, `JWT_SECRET`, `WEB_ORIGIN`, `API_ORIGIN`, `CLOUDFLARE_TUNNEL_TOKEN`, and `DEFAULT_ADMIN_PASSWORD` when seed is intentionally enabled; `npm run validate:production-env -- .env.production --json`, `npm run validate:cloudflare-backend -- --env .env.production --json`, a real non-example signoff based on `docs/production-secrets-signoff.example.json` passes `npm run validate:secrets-signoff -- <signoff.json> --env .env.production --json` or the `commercial-signoff` workflow, `npm run doctor:commercial -- --json`, and release evidence `production-env` / `cloudflare-backend` / `secrets-signoff` all pass with no secret/origin/tunnel blockers. |
| GAP-004 | Open | Infrastructure lead | 2026-06-07 | The file API now supports local persistent volumes and S3-compatible object storage, but a real production bucket/volume, independent backup policy, restore drill, and owner signoff have not been provisioned in this Codex environment; `commercial-signoff` can validate the reviewed storage file once supplied. | Production file storage is backed up independently, restore-tested with `npm run restore:files -- <file-storage-backup.tar.gz> --yes` or platform restore tooling, attachment download smoke passes after restore, and a real non-example signoff based on `docs/file-storage-signoff.example.json` passes `npm run validate:storage-signoff -- <signoff.json> --environment production --json` plus `--file-storage-dir <FILE_STORAGE_DIR>` when `FILE_STORAGE_DRIVER=local`, or passes through the protected `commercial-signoff` workflow. |
| GAP-005 | Open | Product lead | 2026-06-07 | The imported `oa-dashboard.html` personnel dataset has been mapped and masked, but final business sign-off for go-live records, retention, and export policy is still external to the codebase; `commercial-signoff` can validate the reviewed HR/Product file once supplied. | Product/HR signs off source counts, sensitive-field policy, export permissions, and retention rules in a real non-example signoff file based on `docs/hr-data-signoff.example.json`; `npm run validate:hr-signoff -- <signoff.json> --source oa-dashboard.html --json` passes or the protected `commercial-signoff` workflow validates it, and the sign-off reference is attached to release evidence. |
